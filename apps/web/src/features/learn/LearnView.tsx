/**
 * 辅导页。
 *
 * ## 为什么从三栏砍成一栏
 *
 * v1 是「课程树 | 对话 | 状态」三栏。加了侧边栏导航之后，
 * 四个纵向区块挤在 1280px 里，中栏只剩不到 500px —— 而中栏恰恰是
 * 学习者要**读长句子**的地方。这是拿阅读体验去换"信息都在眼前"，
 * 换亏了。
 *
 * v2 的做法是把信息按"什么时候需要"重新分配：
 *
 *   · **选知识点** → 换成一个选择器（没在学时铺在页面上，学习中走弹层）
 *   · **正在学什么 / 到什么程度** → 常驻在对话上方的一条顶栏里
 *   · **对话本身** → 独享整宽，按阅读行宽约束
 *
 * 结果：同一块屏幕里，对话区从不到 500px 变成接近 900px。
 */

import { Stage } from './Stage'

export function LearnView() {
  return (
    <div className="flex h-[calc(100vh-3.5rem)] min-h-0 flex-col">
      <Stage />
    </div>
  )
}
