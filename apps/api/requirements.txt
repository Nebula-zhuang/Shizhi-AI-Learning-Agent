# Learning Buddy · 后端依赖（P0）
# 说明：版本区间宽松，避免锁死；生产化时建议改为精确锁定。

# --- Web 框架 ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
python-multipart>=0.0.12          # 文件上传（P1 使用，P0 先装好）

# --- 配置与校验 ---
pydantic>=2.9,<3.0
pydantic-settings>=2.6,<3.0
python-dotenv>=1.0,<2.0

# --- 数据库 ---
SQLAlchemy>=2.0.36,<3.0
alembic>=1.14,<2.0
PyMySQL>=1.1.1,<2.0
cryptography>=43.0                 # MySQL 8+/9 的 caching_sha2_password 认证需要
PyJWT>=2.8,<3.0                    # JWT 会话令牌签发与校验（app/core/security.py 直接 import jwt）
bcrypt                             # 口令哈希（代码直接 import bcrypt，此前仅靠传递依赖才装上）

# --- LLM 与 HTTP ---
openai>=1.55,<2.0                  # OpenAI 兼容网关，指向 DeepSeek / Qwen / GLM 均可
httpx>=0.27,<1.0

# --- 向量库 ---
chromadb>=0.5.20                   # 进程内 embedded 模式，零运维

# --- 资料解析（P1）---
PyMuPDF>=1.24,<2.0                 # PDF 文本块抽取、内嵌图片提取、图片尺寸读取
python-docx>=1.1,<2.0               # Word(.docx) 段落/标题/表格/内嵌图片抽取
python-pptx>=1.0,<2.0               # PowerPoint(.pptx) 幻灯片/备注/表格/图片抽取

# --- 测试 ---
pytest>=8.3,<9.0
pytest-asyncio>=0.24,<1.0
