# =========================================================================== #
# Learning Buddy · .gitignore
#
# 原则：**可再生的一律不入库**。
#   源码、迁移、测试、文档、Skill、示例资料 → 入库
#   密钥、运行时数据、依赖、构建产物、本地临时文件 → 不入库
# =========================================================================== #

# ---------------------------------------------------------------- 环境与密钥
# `.env` 的**所有变体**都不入库，只保留示例。
# 先写通配再写否定：gitignore 后面匹配的规则覆盖前面的。
.env
.env.*
!.env.example
!.env.*.example

# 其他常见凭据文件形态（本仓库目前没有，写上以防将来手滑）
*.pem
*.key
*.p12
*.pfx
credentials.json
secrets.json

# ---------------------------------------------------------------- Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.hypothesis/

# ---------------------------------------------------------------- Node
node_modules/
# 构建产物。`dist-*` 是构建验证用的备用输出目录
# （本机 `dist/` 有时被宿主程序的临时句柄占住，删不掉）
apps/web/dist/
apps/web/dist-*/
apps/web/.vite/
*.tsbuildinfo
.npm/
.pnpm-store/

# ---------------------------------------------------------------- 运行时数据
# 整个 `data/` 不入库：上传原件、向量库、缓存、日志全是运行时产物。
# 其中 chroma.sqlite3 尤其重要 —— 它体积大、且**多进程访问同一目录会让检索间歇性失败**。
data/*
!data/.gitkeep

# 运行时落盘的测试产物（端到端脚本生成的图、data URI 转储等）
# 它们已经在 `data/*` 里被覆盖，这里再写一遍是为了**意图明确**：
# 这些是临时产物，不是交付物
_browser_upload.png
_scenario_*.png
_scenario_*.txt
_stage22_test.png
_vision_probe.png

# ---------------------------------------------------------------- 本地临时
*.log
!data/logs/.gitkeep
tmp/
.tmp/
*.tmp
*.bak
*.orig
*.rej
*.swp
*~

# 工具产生的临时目录 / 文件
pytest-cache-files-*/
apps/web/vite.config.ts.timestamp-*
.playwright/
.screenshots-tmp/

# 截图/上传工具的**写入权限探针**：内容就是一个 "ok"。
# 它由工具自动生成，不是任何东西的输入 —— 但会被误提交。
# `docs/screenshots/__probe.txt` 就是这么来的。
__probe.txt
**/__probe.txt

# ---------------------------------------------------------------- 编辑器与系统
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# ---------------------------------------------------------------- 工作区记忆
# 助手的工作记忆：含本机绝对路径、内部排查笔记、中间结论。
# **不适合公开仓库**（噪音大，且容易夹带机器相关信息）。
# 如果你想把它当作开发日志一起发布，删掉下面这一行即可。
.learnbuddy/

# ---------------------------------------------------------------- 一次性排查脚本
# `scripts/_*.py` 是开发期临时脚本，不是交付物。
# 但 `scripts/verify_*.py` 与 `scripts/*_smoke.py` **是交付物，必须入库**。
scripts/_*.py
scripts/_scratch/

# ---------------------------------------------------------------- 示例资料
# `samples/` 由 `scripts/make_samples.py` 生成，**可以再生**。
# 但仍然入库 —— 它让 clone 下来就能直接演示完整链路，价值大于几百 KB 的体积。
# 如果你希望保持仓库纯净，取消下面一行的注释即可：
# samples/

# ---------------------------------------------------------------- 非 Shizhi 的并行产物
# 初赛 PPT 制作那条工作线产生的东西：CloudStudio 工作区配置 + 一个**独立的** PPT 演示前端
# + PPT 生成工具的中间产物。
#
# ⚠️ 它们**不是 Shizhi 源码**，判定依据（硬证据）：
#   · `frontend/package.json` 的 `name` 是 `ppt-frontend`，而项目前端 `apps/web` 是 `shizhi-web`
#   · `frontend/` 用 `vite.config.js`（项目是 `.ts`），且**没有** `src/main.tsx` / `src/App.tsx`
#   · `frontend/README.md` 自己写着「# PPT Frontend - Vite Project」
#   · 仓库已跟踪文件中，这些路径被引用 **0 处**
#
# 它们**保留在本地**，只是不参与版本控制 —— 尤其 `frontend/`：
# 含自己的 `node_modules`，合计 3878 个文件 / 约 46.7 MB，一旦 `git add -A` 就会被整包推上去。
#
# 用 `/` 开头的锚定写法：只匹配仓库根目录下这些路径，避免误伤将来可能出现的同名子目录。
/.cloudstudio
/frontend/
/artifacts/
/docs/project.json
/docs/page-global-config.json
/docs/pages.json
/docs/product/
